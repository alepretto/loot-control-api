meta {
  name: Delete Account
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/accounts/{{account_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
