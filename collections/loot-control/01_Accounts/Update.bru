meta {
  name: Update Account
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/accounts/{{account_id}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{auth_token}}
}

body:json {
  {
    "label": "Nubank Updated",
    "type": "digital"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
