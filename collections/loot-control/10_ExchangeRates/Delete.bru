meta {
  name: Delete Exchange Rate
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/exchange-rates/{{exchange_rate_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
