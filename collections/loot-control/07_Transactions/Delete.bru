meta {
  name: Delete Transaction
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/transactions/{{transaction_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
