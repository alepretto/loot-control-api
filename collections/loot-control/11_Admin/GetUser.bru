meta {
  name: Get User (Admin)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/users/{{user_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
