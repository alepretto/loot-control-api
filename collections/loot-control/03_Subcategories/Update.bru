meta {
  name: Update Subcategory
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/subcategories/{{subcategory_id}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{auth_token}}
}

body:json {
  {
    "label": "Restaurante e Delivery",
    "is_active": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
