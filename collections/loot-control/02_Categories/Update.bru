meta {
  name: Update Category
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/categories/{{category_id}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{auth_token}}
}

body:json {
  {
    "label": "Alimentação e Mercado",
    "nature": "variable"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
